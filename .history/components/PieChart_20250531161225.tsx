import React from 'react'
interface StatusCount {
    pending: number,
    inProgress: number,
    completed: number
}
function PieChart({ statusData: { statusData: statusCounts } }) {
    return (
        <div>

        </div>
    )
}

export default PieChart
