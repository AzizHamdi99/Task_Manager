import React from 'react'
interface StatusCount {
    pending: number,
    inProgress: number,
    completed: number
}
function PieChart({ statusData: { } }) {
    return (
        <div>

        </div>
    )
}

export default PieChart
